class Weather {
    constructor(temp, frequency, intensity) {
        this.temperature = temp;
        this.precipitationFrequency = frequency;
        this.precipitationIntensity = intensity;
    }
}